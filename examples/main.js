const [UP, DOWN] = ['UP', 'DOWN']
const state = {
  direction: UP
}

// main
export default sphere => {
  // if (sphere.position.y <= 5) {
  //   state.direction = UP
  // } else if (sphere.position.y >= 15) {
  //   state.direction = DOWN
  // }
  //
  // if (state.direction === UP) {
  //   sphere.position.y += 0.02
  // } else {
  //   sphere.position.y -= 0.02
  // }
}
